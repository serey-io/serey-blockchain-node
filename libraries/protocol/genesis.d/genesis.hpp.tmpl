/*****************************************
 *                                       *
 * This file is automatically generated. *
 * To create different genesis values,   *
 * please modify the files in genesis.d  *
 * instead of modifying this file.       *
 *                                       *
 *****************************************/
#pragma once
#include <fc/io/json.hpp>



#define MAKE_GENESIS_REWARD_FUND() fc::json::from_string( \
  {% for line in genesis_reward_fund -%} 
    "{{ line }}" \
  {% endfor -%} 
);

#define MAKE_GENESIS_GLOBAL_PROPERTIES() fc::json::from_string( \
  {% for line in genesis_global_properties -%}              
    "{{ line }}" \
  {% endfor -%}
);                                                          

#define MAKE_GENESIS_ACCOUNTS() fc::json::from_string( \
  {% for line in genesis_accounts -%}                       
    "{{ line }}" \
  {% endfor -%}                                             
);

